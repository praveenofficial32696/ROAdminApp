import { Booking } from './types';

export const INITIAL_BOOKINGS: Booking[] = [];

export const INITIAL_TECHNICIANS: { name: string, phone: string }[] = [];

export const REGISTERED_CUSTOMERS: { name: string, phone: string }[] = [];
